services:
  h2o:
    container_name: h2o
    image: sipcink/h2o:latest
    environment:
      TZ: Asia/Shanghai
      # 纯 IP+端口 HTTP 部署时取消注释，否则浏览器不会存储 session cookie
      # H2O_SECURE_COOKIE: "false"
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
